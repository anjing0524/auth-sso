# ============================================
# Auth-SSO Portal — 生产环境变量模板（Docker Compose 部署）
# ============================================
# 使用：cp .env.prod.example .env.prod → 编辑 .env.prod
# 校验：packages/config/src/env.ts (Zod schema)
# 必填：DATABASE_URL, BETTER_AUTH_SECRET, TRUSTED_ORIGINS
#       另有 POSTGRES_PASSWORD, REDIS_PASSWORD（容器层，非应用代码）
# ============================================

# ── 运行环境 ──────────────────────────────
NODE_ENV=production
LOG_LEVEL=info

# ── 应用 URL（必须替换为外网可访问域名） ──
BETTER_AUTH_URL=https://portal.yourdomain.com
NEXT_PUBLIC_APP_URL=https://portal.yourdomain.com

# ── 密钥（必填 *，≥32字符） ───────────────
# 生成命令：openssl rand -base64 32
BETTER_AUTH_SECRET=your_better_auth_secret_must_be_generated_min_32_chars

# ── PostgreSQL 容器配置 ──────────────────
# 以下供 docker-compose.prod.yml 使用，非应用代码直接读取
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_highly_secure_db_password
POSTGRES_DB=auth_sso

# 应用连接串（主机名使用 Docker 网络内部解析：postgres）
DATABASE_URL=postgresql://postgres:your_highly_secure_db_password@postgres:5432/auth_sso

# ── Redis 容器配置 ───────────────────────
REDIS_PASSWORD=your_highly_secure_redis_password

# 应用连接串（主机名使用 Docker 网络内部解析：redis）
REDIS_URL=redis://:your_highly_secure_redis_password@redis:6379

# ── OIDC Provider（通常无需单独设置） ────
# PORTAL_ISSUER=https://portal.yourdomain.com
# PORTAL_JWKS_URI=https://portal.yourdomain.com/api/auth/.well-known/jwks

# ── Portal 自身 OAuth Client ──────────────
NEXT_PUBLIC_CLIENT_ID=portal
PORTAL_CLIENT_ID=portal
PORTAL_CLIENT_SECRET=your_portal_client_secret_must_be_generated

# ── CORS（必填 *，逗号分隔）───────────────
# 包含 Portal + 所有接入的 SSO 客户端域名
TRUSTED_ORIGINS=https://portal.yourdomain.com,https://demo.yourdomain.com

# ── Session TTL ───────────────────────────
SESSION_MAX_AGE_SEC=604800
SESSION_IDLE_TIMEOUT_SEC=86400
