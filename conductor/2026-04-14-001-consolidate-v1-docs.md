# 文档合并与重构计划 - v1.0 规范化

## 1. 背景与目标
当前项目根目录下散落了大量的 `v1.0` 初始设计文档（PRD、总方案、技术选型、数据库草案、接口清单、排期等）。为了符合软件工程规范，提高文档的可维护性和可读性，需要将这些文档按照功能模块合并、重构并转移至 `docs/` 目录下，随后清理根目录。

## 2. 合并映射与重构策略

根据软件工程标准文档体系，执行以下合并：

### 2.1 产品需求 (PRD)
* **目标文件**: `docs/PRD.md`
* **合并来源**:
  * `PRD-auth-sso-v1.0.md`
* **重构要求**: 保持原需求结构，优化格式和描述清晰度。

### 2.2 系统架构设计 (Architecture)
* **目标文件**: `docs/ARCHITECTURE.md`
* **合并来源**:
  * `冻结评审版-总方案-auth-sso-v1.0.md`
  * `技术选型-auth-sso-v1.0.md`
  * `技术探针-Better-Auth-OIDC-Provider-v1.0.md`
* **重构要求**: 整合业务方案与技术选型，将技术探针结果作为技术决策依据并入架构文档。

### 2.3 数据库设计 (Database Schema)
* **目标文件**: `docs/DATABASE.md`
* **合并来源**:
  * `数据库表结构草案-auth-sso-v1.0.md`
* **重构要求**: 规范化表结构描述，去除“草案”等临时性字眼，作为标准数据库设计基线。

### 2.4 接口契约 (API Contract)
* **目标文件**: `docs/API.md`
* **合并来源**:
  * `接口清单-字段级契约-auth-sso-v1.0.md`
* **重构要求**: 按模块分类整理 API，确保参数和响应描述准确。

### 2.5 项目与实施计划 (Project Plan)
* **目标文件**: `docs/PROJECT_PLAN.md`
* **合并来源**:
  * `研发实施清单-里程碑-auth-sso-v1.0.md`
  * `项目排期与任务清单-auth-sso-v1.0.md`
  * `开发周期清单-auth-sso-v1.0.md`
* **重构要求**: 整合里程碑、任务拆解与排期时间线，形成统一的项目管理视图。

### 2.6 评审导读 (Review Guide)
* **目标文件**: (废弃或归档)
* **合并来源**: `评审阅读顺序-auth-sso-v1.0.md`
* **重构要求**: 此文件为过程性文件，可直接删除或将其精华摘要并入 `README.md` 或其他索引中。

## 3. 实施步骤

1. **创建与合并**: 在 `docs/` 目录下创建上述 5 个目标 Markdown 文件，并使用合并策略提取对应来源文件的内容，进行排版与校对。
2. **清理源文件**: 删除根目录下对应的源文件。
3. **更新索引**: 运行 `document-release` skill（或手动执行该工作流），更新 `README.md` 等核心索引文件，确保其指向新的 `docs/` 下的文档路径。
4. **提交代码**: 创建 `docs: consolidate v1.0 design documents into standard structure` 的提交。

## 4. 验证标准
* 根目录下不再有 `*-auth-sso-v1.0.md` 系列散落文件。
* `docs/` 目录下包含结构清晰的 `PRD.md`, `ARCHITECTURE.md`, `DATABASE.md`, `API.md`, `PROJECT_PLAN.md`。
* 文档内部无矛盾，语言专业、清晰。
* 所有外部引用链接正确。